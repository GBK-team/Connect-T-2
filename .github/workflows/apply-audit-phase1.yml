name: Apply Production Audit Phase One

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Phase one has already been applied to the audit branch."
